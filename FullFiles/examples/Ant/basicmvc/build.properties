src.dir=src${file.separator}main${file.separator}java
webapp.dir=src${file.separator}main${file.separator}webapp
webinf.dir=${webapp.dir}${file.separator}WEB-INF
lib.dir=${webinf.dir}${file.separator}lib
build.dir=build
classes.dir=${build.dir}${file.separator}classes
dist.dir=${build.dir}${file.separator}dist
java.required.version=9
