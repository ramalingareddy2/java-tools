task "message" {
  doLast {
    //println "Hurray! Gradle build works!"
    println "Hey! wrote java code in Groovy Gradle Build Script!!!"
    /*File f = new File("D:/transcripts/TIMINGS");
    FileInputStream fis = new FileInputStream(f);
    Integer c=0;
    while((c = fis.read())!=-1) {
      System.out.print((char) c);
    }
    fis.close();*/
  }
}